var radius = prompt("Enter radius value (r):");
var height = prompt("Enter height value (h):");
const pi = 3.1415;

var volume = pi * parseInt(radius) * parseInt(radius) * parseInt(height);

alert("Volume of cylinder is: " + volume);